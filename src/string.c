// Online C compiler to run C program online
#include <stdio.h>

int main() {
    // Write C code here
    char name[100];
    fgets(name,sizeof(name),stdin);
    printf("Try programiz.pro");

    return 0;
}